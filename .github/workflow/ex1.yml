name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "My first Github Actions job"
        
        - name: list files
          run: ls
        
        - name: read file
          run: cat README.md

        - name: read file2
          run: cat README.md